conda create -n vmodel
conda activate vmodel

# from astropy docs
conda install astropy
conda install -c astropy -c defaults scipy h5py beautifulsoup4 html5lib bleach pyyaml pandas sortedcontainers pytz matplotlib setuptools mpmath bottleneck jplephem asdf
pip3 install pytest-astropy sphinx-astropy
# run astropy.test() in interpreter - tests passed except one with known issue

# sbpy requirement
pip install --upgrade --pre astroquery
# astroquery seems to work

pip install --upgrade --pre sbpy
